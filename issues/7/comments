Hi @TheDauntless,

Thank you for reporting this issue.

After a thorough investigation of the OWASP/mastg repository, I found that the file `docs/hooks/checklist_utils.py` does not currently exist. I performed multiple searches including:
- Direct file path lookup
- Directory listing for `docs/hooks/`
- Semantic and lexical code searches across the entire repository

The only implementation of these functions (`retrieve_masvs`, `get_mastg_tests_dict`, `get_checklist_dict`, etc.) exists in `src/scripts/combine_data_for_checklist.py`, which is imported by `src/scripts/yaml_to_excel.py`.

**There is no duplicate code to refactor** in the current state of the repository.

Possible explanations:
- The file may have already been removed or refactored
- The issue description may have been based on outdated information
- The file path might have been incorrect

If you believe there's still duplicate code somewhere else or have more details about this issue, please provide updated information and I'll be happy to investigate further.

Closing this issue as it's not applicable to the current codebase.