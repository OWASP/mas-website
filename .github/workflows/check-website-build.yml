name: Check Website Build
on:
  pull_request:
    branches:
      - "*"

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/build-website-reusable.yml
    with:
      deploy: false